replicaCount: 1

image:
  repository: app-notas
  tag: "1.0"
  pullPolicy: IfNotPresent

app:
  port: 5000
  env: "dev"

service:
  type: ClusterIP
  port: 5000

persistence:
  enabled: true
  accessMode: ReadWriteOnce
  size: 1Gi
  mountPath: /data
